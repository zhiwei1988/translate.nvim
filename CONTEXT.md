# CONTEXT

translate.nvim 的领域术语表。只收录已达成共识的词汇,不含实现细节。

## 术语

- **段落(Paragraph)**:悬浮翻译的基本单位,即一个完整的**可译块**。在 Markdown 文件里由文档结构决定边界——一个散文块、一整个列表(含其全部嵌套项)、一张表格、一个引用块、一个标题各自成段;在纯文本文件里退化为空行分隔的连续文本块。
- **可译块(Translatable Block)**:可以作为一个段落整体送去翻译的内容单元。
- **不可翻译块(Untranslatable Block)**:内容不构成散文、因而永不单独送翻的块——代码块、HTML 块、分隔线。光标落在其中时不发起翻译。它仍可作为某个可译块的一部分被一并送出(例如列表项里的代码块),此时要求原样保留、不译。
- **选区翻译(Selection Translation)**:以 visual 选区而非段落为单位的翻译。边界完全由用户给定,不做块类型判断,也不扩展到整行。
- **Adapter(适配器)**:某一种 API 协议的实现,负责构造请求、发出、把响应流解析成文本增量、以及被中止。它对缓存、重试、并发一无所知。
- **Preset(厂商预设)**:架在 adapter 之上、描述某个具体服务的配置集合——接入地址、认证方式、默认模型、参数差异。
- **Provider(翻译引擎)**:用户视角下的一个可选翻译来源。绝大多数 provider 只是一个 preset;只有对接协议不兼容的服务时,才需要一个完整的 adapter。
- **翻译调度层(Translator)**:位于 provider 之上的一层,负责缓存、宽限期、在途请求、并发闸门与重试。provider 与它是单向关系——provider 不知道它的存在。
- **悬浮译文窗(Translation Float)**:锚定在段落附近、流式渲染译文的浮动窗口。**它从不接受焦点**——用户的光标始终留在原文里,浮窗关闭与否只由光标是否还在段落内决定。因而"翻阅超长译文"不能靠进入这个窗口来完成。
- **手动触发(Manual Trigger)**:用户按 keymap 对光标所在段落或 visual 选区发起翻译。**翻译只由此发起**——不存在光标停留后自动翻译的模式,每一次请求都对应一次用户的明确意图。
- **目标语言(Target Language)**:译文语言,可配置,默认中文;源语言不配置,由 LLM 自动识别。以**自然语言名**表达而非语言代码,因而能表达代码体系表达不了的变体(繁体中文、粤语、某种方言腔调)。
- **生效配置(Effective Configuration)**:一次 nvim 会话内固定不变的那组设置——provider、model、目标语言与 prompt。**运行时不可切换**,改配置须重启。因此缓存键的每一个组成部分在一次会话内都是常量。
- **附加指令(Extra Instructions)**:用户追加在内置 prompt 之后的自定义要求——固定术语译法、地区用语偏好、领域行话。它是自定义翻译行为的**首选方式**,因为内置 prompt 里那些保障正确性的约束得以保留。另有全量替换整个 prompt 的逃生舱,代价是那些约束一并消失。
- **术语双写(Term Gloss)**:技术术语在一个段落内**首次出现**时,以「译名(English)」的形式呈现,其后各次只用译名。因为每个段落都是独立翻译的,同一术语在不同段落会各自双写一次——这在逐段随机阅读的场景下是特性而非冗余。
- **规范化原文(Normalized Source)**:原文经空白规范化后的形式。同一段文字在不同折行方式下会得到相同的规范化原文,因而被视为同一份内容。仅用于构成缓存键,送往 provider 的始终是原始段落。**只适用于散文段落**——列表、表格以及任何含不可翻译块的内容,其行结构本身即内容,一律以原始字节构成缓存键。
- **缓存键(Cache Key)**:唯一标识一份译文的复合标识,由规范化原文、目标语言、provider、model 与 **Prompt 指纹**共同决定。任一项不同即是不同的缓存条目。
- **Prompt 指纹(Prompt Fingerprint)**:最终 system prompt 的内容摘要。它进入缓存键,使 prompt 变更自动令旧译文失效,无需人工维护版本号。**它只随配置变化,绝不随被翻译的内容变化**——原文一旦进入 system prompt,指纹就变成每段一个值,缓存键里多出一个恒变项,缓存即告全废。
- **缓存条目(Cache Entry)**:一份已完成的译文及其自描述元信息。只有完整译文才成为条目——部分译文永不入缓存。
- **在途请求(In-flight Request)**:已发出、尚未完成的翻译请求。按缓存键索引,使同一段落的重复触发得以复用同一个请求而非重复发起。
- **宽限期(Grace Period)**:悬浮译文窗关闭后、请求被真正取消之前的一段时间。期间完成的译文会静默成为缓存条目,以回收已经产生的开销。
