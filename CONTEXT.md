# CONTEXT

translate.nvim 的领域术语表。只收录已达成共识的词汇,不含实现细节。

## 术语

- **段落(Paragraph)**:空行分隔的连续文本块,是悬浮翻译的基本单位。边界情况(代码块、列表等)的语义见工单「段落检测与选区语义」,落定后回写此处。
- **Provider(翻译引擎)**:对某个 LLM API 的适配器,实现统一的翻译接口。内置 OpenAI-compatible 与 Gemini 两种;用户可注册自定义 provider。
- **悬浮译文窗(Translation Float)**:锚定在段落附近、流式渲染译文的浮动窗口。
- **手动触发(Manual Trigger)**:用户按 keymap 对光标所在段落发起翻译,是默认交互。
- **自动触发(Auto Trigger)**:`CursorHold` 驱动的可选模式,光标停留后自动翻译。
- **目标语言(Target Language)**:译文语言,可配置,默认中文;源语言不配置,由 LLM 自动识别。
- **规范化原文(Normalized Source)**:原文经空白规范化后的形式。同一段文字在不同折行方式下会得到相同的规范化原文,因而被视为同一份内容。仅用于构成缓存键,送往 provider 的始终是原始段落。
- **缓存键(Cache Key)**:唯一标识一份译文的复合标识,由规范化原文、目标语言、provider、model 与 **Prompt 指纹**共同决定。任一项不同即是不同的缓存条目。
- **Prompt 指纹(Prompt Fingerprint)**:最终 system prompt 的内容摘要。它进入缓存键,使 prompt 变更自动令旧译文失效,无需人工维护版本号。
- **缓存条目(Cache Entry)**:一份已完成的译文及其自描述元信息。只有完整译文才成为条目——部分译文永不入缓存。
- **在途请求(In-flight Request)**:已发出、尚未完成的翻译请求。按缓存键索引,使同一段落的重复触发得以复用同一个请求而非重复发起。
- **宽限期(Grace Period)**:悬浮译文窗关闭后、请求被真正取消之前的一段时间。期间完成的译文会静默成为缓存条目,以回收已经产生的开销。
